> **AUTO-GENERATED — DO NOT EDIT — re-run `scripts/generate-hazbot-validation-playbook.js`**


# Validation Playbook — Rule Set 25

## Factor Variables

- **ranSimulation** — At least one "SimulationStarted" event was recorded.
- **TwoSparks** — Simulation prop for whether two sparks were used.
  - Details: The sparks array must be of length 2 for this variable to be true. If false, then it means that there was exactly one spark in the simulation.
- **OneSparkPerZone** — Simulation prop for whether one spark was used per each zone or not.
  - Details: The sparks array must be of length 2, and the two zoneIdx values collected from the two sparks must cover 0 (zone 1) and 1 (zone 2). <j> means taking the spark index (the index of the sparks data, which is, or must be, an array), 0-based.
- **SparksAtTopAndBottom** — Simulation prop for whether or not one spark was at or near the top (ridge), while the other spark was at or near the bottom (valley).
  - Details: The sparks array must be of length 2, and the x,y,elevation values for the sparks must be such that one spark is near/at the ridge, while the other spark is near/at the valley. The code needs to be written based on the topograhy map used and the x, y, elevaation values for the spark locations. One way to do it would be pre-trace the ridge lines and the valley lines and determine if the spark locations are close enough to them (this work never done before; Alert: new algorithm coding required here).
- **UniformZoneSettings** — Simulation prop for whether all zones were set up the same way
  - Details: For the two zones (i=0 and i=1), the droughtLevel values must be the same, as must be the vegetation values. The terrainType values should be the same by design (see the "SIMINIT" sheet).

## Categories

### Category 1: Did not run the simulation A.k.a. Click button (before they do anything else)

- **Feedback**: Hazbot: I will analyze your model after you run it. Scroll up to see the instructions at the top of the page! [Okay]
- **Expression**: `NOT ranSimulation`
- **Logical breakdown**:
  - NOT:
    - `ranSimulation` is true

### Category 2: Ran the simulation with only one spark

- **Feedback**: Hazbot: I only see 1 spark. Make sure each zone has a spark! [Show me]
- **Expression**: `ranSimulation WITH NOT TwoSparks`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - NOT:
      - sim-prop `TwoSparks` is true

### Category 3: Ran the simulation with two sparks in the same zone.

- **Feedback**: Hazbot: I see 2 sparks in the same zone. Let's make sure that each zone has 1 spark! [Show me]
- **Expression**: `ranSimulation WITH NOT OneSparkPerZone AND TwoSparks`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - NOT:
        - sim-prop `OneSparkPerZone` is true
      - sim-prop `TwoSparks` is true

### Category 4: Ran the simulation and placed two sparks, but not at the top and bottom of the mountain

- **Feedback**: Hazbot: Make sure to place one spark at the bottom of a mountain and one at the top! [Show me]
- **Expression**: `ranSimulation WITH OneSparkPerZone AND NOT SparksAtTopAndBottom`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - sim-prop `OneSparkPerZone` is true
      - NOT:
        - sim-prop `SparksAtTopAndBottom` is true

### Category 5: Ran the simulation with one spark at the top and one at the bottom of the mountain but with different zone setups.

- **Feedback**: Hazbot: Looks like the two zones are different. Make sure they have the same vegetation and drought! [Show me]
- **Expression**: `ranSimulation WITH OneSparkPerZone AND SparksAtTopAndBottom AND NOT UniformZoneSettings`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - ALL of:
        - sim-prop `OneSparkPerZone` is true
        - sim-prop `SparksAtTopAndBottom` is true
      - NOT:
        - sim-prop `UniformZoneSettings` is true

### Category 6: Ran the simulation with one spark at the top and one at the bottom of the mountain with the same zone setups.

- **Feedback**: Hazbot: Great job! You’re ready to answer the questions below. [Hooray!]
- **Expression**: `ranSimulation WITH OneSparkPerZone AND SparksAtTopAndBottom AND UniformZoneSettings`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - ALL of:
        - sim-prop `OneSparkPerZone` is true
        - sim-prop `SparksAtTopAndBottom` is true
      - sim-prop `UniformZoneSettings` is true
