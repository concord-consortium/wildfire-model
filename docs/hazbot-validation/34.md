> **AUTO-GENERATED — DO NOT EDIT — re-run `scripts/generate-hazbot-validation-playbook.js`**


# Validation Playbook — Rule Set 34

## Factor Variables

- **ranSimulation** — At least one "SimulationStarted" event was recorded.
- **VegetationSet** — Sim prop for vegetation set distinct from the default value for any zone.
  - Details: Vegetation is set per zone, in the zones data. For the default values, read the "SIMINIT" sheet.
- **DroughtLevelSet** — Sim prop for drought level set distinct from the default value for any zone.
  - Details: Drought level is set per zone, in the zones data. For the default values, read the "SIMINIT" sheet.
- **WindSet** — Sim prop for any wind parameter set  distinct from the default value.
  - Details: Wind is set globally (for all zones). For the default values, read the "SIMINIT" sheet. Allow this sim prop to evalue to true when direction is set away from the default value even if the magnitude was set to zero. Here, the "magnitude" means the wind speed as displayed in the simulation (like "10" as in "10 MPH"). In the log data, the magnitude data entails two fields "wind.speed" and "wind.scaleFactor". The "magnitude" is computed as "wind.speed" / "wind.scaleFactor". Any small change should be accepted.

## Categories

### Category 1: Did not run the simulation. A.k.a. Click button (before they do anything else)

- **Feedback**: Hazbot: Just a friendly reminder, I will analyze your model after you run it. **Scroll up** to see the instructions at the top of the page! [Okay]
- **Expression**: `NOT ranSimulation`
- **Logical breakdown**:
  - NOT:
    - `ranSimulation` is true

### Category 2: Ran the simulation(s) with default setup values only

- **Feedback**: Hazbot: Looks like you haven’t changed the **Setup** yet. What conditions do you think will produce a **high intensity fire**? [Show me]
- **Expression**: `ranSimulation WITH NOT VegetationSet AND NOT (WindSet OR DroughtLevelSet)`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - NOT:
        - sim-prop `VegetationSet` is true
      - NOT:
        - ANY of:
          - sim-prop `WindSet` is true
          - sim-prop `DroughtLevelSet` is true

### Category 3: Ran the simulation, with drought or wind changed but vegetation unchanged.

- **Feedback**: Hazbot: Keep experimenting! Try different types of vegetation to produce an even more **high intensity fire**? [Show me]
- **Expression**: `ranSimulation WITH NOT VegetationSet AND (WindSet OR DroughtLevelSet)`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - NOT:
        - sim-prop `VegetationSet` is true
      - ANY of:
        - sim-prop `WindSet` is true
        - sim-prop `DroughtLevelSet` is true

### Category 4: Ran the simulation, with vegetation changed but not wind or drought changed.

- **Feedback**: Hazbot: Great job experimenting with different types of vegetation. Try changing the drought level or wind to produce an even more **high intensity fire!** [Show me]
- **Expression**: `ranSimulation WITH VegetationSet AND NOT (WindSet OR DroughtLevelSet)`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - sim-prop `VegetationSet` is true
      - NOT:
        - ANY of:
          - sim-prop `WindSet` is true
          - sim-prop `DroughtLevelSet` is true

### Category 5: Ran the simulation with vegetation changed in at least one zone and either wind or drought changed in any zone.

- **Feedback**: Hazbot: Great job! You’re ready to answer the questions below. [Hooray!]
- **Expression**: `ranSimulation WITH VegetationSet AND (WindSet OR DroughtLevelSet)`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - sim-prop `VegetationSet` is true
      - ANY of:
        - sim-prop `WindSet` is true
        - sim-prop `DroughtLevelSet` is true
