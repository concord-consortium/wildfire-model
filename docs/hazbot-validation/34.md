> **AUTO-GENERATED — DO NOT EDIT — re-run `scripts/generate-hazbot-validation-playbook.js`**


# Validation Playbook — Rule Set 34

## Factor Variables

- **simulationRuns** — Simulation runs that are initiated by "SimulationStarted."
  - Details: Each simulation run is started by 'SimulationStarted', and how it ends is determined by various events listed here.
- **sawIntenseFire** — High fire intensity (red) was clearly observed in the simulation outcome.
  - Details: Alert: new feature needed. The 'outcome' field could indicate whether high intensity (red, not orange) appeared significantly in the image generated by the simulation.
- **All other factor variables used above are the same as those used in 33 and are omitted here fore brevity.** — (no definition)

## Categories

### Category 1: Did not run the simulation

- **Feedback**: Hazbot: You haven’t run the model yet. Run it before answering the questions.
- **Expression**: `simulationRuns.length == 0`
- **Logical breakdown**:
  - `simulationRuns.length == 0`

### Category 2: Ran the simulation but did not change any variables (used default values only)

- **Feedback**: Hazbot: Click “Setup” and set the conditions in each zone to experiment with creating a high intensity fire.
- **Expression**: `simulationRuns.length > 0 AND NOT setAnyVar`
- **Logical breakdown**:
  - ALL of:
    - `simulationRuns.length > 0`
    - NOT:
      - `setAnyVar` is true

### Category 3: Ran the simulation without changing vegetation

- **Feedback**: Hazbot: Try different types of vegetation to see which one produces an intense fire!
- **Expression**: `simulationRuns.length > 0 AND setAnyVar AND NOT setVegetation`
- **Logical breakdown**:
  - ALL of:
    - ALL of:
      - `simulationRuns.length > 0`
      - `setAnyVar` is true
    - NOT:
      - `setVegetation` is true

### Category 4: Ran and never sees an intense fire.

- **Feedback**: Hazbot: Try changing other factors like drought level or wind to make an intense fire!
- **Expression**: `simulationRuns.length > 0 AND setVegetation AND NOT sawIntenseFire`
- **Logical breakdown**:
  - ALL of:
    - ALL of:
      - `simulationRuns.length > 0`
      - `setVegetation` is true
    - NOT:
      - `sawIntenseFire` is true

### Category 5: Ran the simulation two or more times, changed the vegetation at least once, and saw intense fire.

- **Feedback**: Hazbot: Great job! You’re ready to answer the questions below.
- **Expression**: `simulationRuns.length > 1 AND setVegetation AND sawIntenseFire`
- **Logical breakdown**:
  - ALL of:
    - ALL of:
      - `simulationRuns.length > 1`
      - `setVegetation` is true
    - `sawIntenseFire` is true
