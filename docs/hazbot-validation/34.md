> **AUTO-GENERATED — DO NOT EDIT — re-run `scripts/generate-hazbot-validation-playbook.js`**


# Validation Playbook — Rule Set 34

## Factor Variables

- **ranSimulation** — At least one "SimulationStarted" event was recorded.
- **setVegetation** — There is at least one "SimulationStarted" event for which the vegetation type was set distinct from the default value for any zone.
  - Details: Vegetation is set per zone, in the zones data. For the default values, read the "SIMINIT" sheet.
- **setDroughtLevel** — There is at least one "SimulationStarted" event for which the drought level was set distinct from the default value for any zone.
  - Details: Drought level is set per zone, in the zones data. For the default values, read the "SIMINIT" sheet.
- **setAnyZoneVar** — setVegetation OR setDroughtLevel
  - Details: Note that the terrainType values are fixed (see the "SIMINIT" sheet).
- **setWind** — There is at least one "SimulationStarted" event for which the wind value was set distinct from the default value for any zone.
  - Details: Wind is set globally (for all zones). For the default values, read the "SIMINIT" sheet. If the magnitude is 0, then the direction has no effect and must be ignored. So set the direction to null, if the magnitude is 0. Here, the "magnitude" means the wind speed as displayed in the simulation (like "10" as in "10 MPH"). In the log data, the magnituide data entails two fields "wind.speed" and "wind.scaleFactor". The "magitude" is computed as "wind.speed" / "wind.scaleFactor".
- **setAnyVar** — setAnyZoneVar OR setWind
- **triedAllVegetations** — Over the course of all simulation runs, tried all vegetation values.
  - Details: The set of all vegetation values found in all zones of all simulations run thus far must be equal to the set of all possible vegetation values ("Grass", "Shrub", "Forest", "Forest with Suppression"). (The order in which these vegetation values appear is unimportant.)

## Categories

### Category 1: Did not run the simulation. A.k.a. Click button (before they do anything else)

- **Feedback**: Hazbot: Just a friendly reminder, I will analyze your model after you run it. Scroll up to see the instructions at the top of the page! [Okay]
- **Expression**: `NOT ranSimulation`
- **Logical breakdown**:
  - NOT:
    - `ranSimulation` is true

### Category 2: Ran the simulation(s) with default setup values only

- **Feedback**: Hazbot: Looks like you haven’t changed the Setup yet. What conditions do you think will produce a high intensity fire? [Show me]
- **Expression**: `ranSimulation AND NOT setAnyVar`
- **Logical breakdown**:
  - ALL of:
    - `ranSimulation` is true
    - NOT:
      - `setAnyVar` is true

### Category 3: Ran the simulation, with drought and wind changed but vegetation unchanged.

- **Feedback**: Hazbot: Keep experimenting! What type of vegetation do you think will produce a high intensity fire? [Show me]
- **Expression**: `setDroughtLevel AND setWind AND NOT setVegetation`
- **Logical breakdown**:
  - ALL of:
    - ALL of:
      - `setDroughtLevel` is true
      - `setWind` is true
    - NOT:
      - `setVegetation` is true

### Category 4: Ran the simulation two or more times, tested all four vegetation types, and changed drought and winds.

- **Feedback**: Hazbot: Great job! You’re ready to answer the questions below.
- **Expression**: `setDroughtLevel AND setWind AND triedAllVegetations`
- **Logical breakdown**:
  - ALL of:
    - ALL of:
      - `setDroughtLevel` is true
      - `setWind` is true
    - `triedAllVegetations` is true
