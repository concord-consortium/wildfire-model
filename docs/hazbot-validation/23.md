> **AUTO-GENERATED — DO NOT EDIT — re-run `scripts/generate-hazbot-validation-playbook.js`**


# Validation Playbook — Rule Set 23

## Factor Variables

- **ranSimulation** — At least one "SimulationStarted" event was recorded.
- **setTerrainType** — There is at least one "SimulationStarted" event for which the terrain type was set distinct from the default value for any zone.
  - Details: Terrain type is set per zone, in the zones data. Default values (see the "SIMINIT" sheet) = "Plains" (zone 1), "Plains" (zone 2). <i> means taking the zone index i (the index for the zones data, which is, or must be, an array), 0-based. So i=0 means zone 1 and i=1 means zone 2. If true, then neecessarily ranSimulation=true.
- **setVegetation** — There is at least one "SimulationStarted" event for which the vegetation type was set distinct from the default value for any zone.
  - Details: Vegetation is set per zone, in the zones data. For the default values, read the "SIMINIT" sheet. If true, then neecessarily ranSimulation=true.
- **setDroughtLevel** — There is at least one "SimulationStarted" event for which the drought level was set distinct from the default value for any zone.
  - Details: Drought level is set per zone, in the zones data. For the default values, read the "SIMINIT" sheet. If true, then neecessarily ranSimulation=true.
- **setAnyZoneVar** — setTerrainType OR setVegetation OR setDroughtLevel
- **OneSparkPerZone** — Simulation prop for whether one spark was used per each zone or not.
  - Details: The sparks array must be of length 2, and the two zoneIdx values collected from the two sparks must cover 0 (zone 1) and 1 (zone 2). <j> means taking the spark index (the index of the sparks data, which is, or must be, an array), 0-based.
- **CorrectZoneSetup** — Simulation prop for whether all zones were correctly set up.
  - Details: The correct zone settings for this activity are as follows. For zone 1 (i=0): terrainType "Foothills", vegetation "Grass", and droughtLevel "No Drought". For zone 2 (i=1): terrainType "Foothills", vegetation "Grass", and droughtLevel "Mild Drought" or "Medium Drought".

## Categories

### Category 1: Did not run the simulation. A.k.a. Click button (before they do anything else)

- **Feedback**: Hazbot: I will analyze your model after you run it! Did you see the instructions at the top of the page? Scroll up! [Okay]
- **Expression**: `NOT ranSimulation`
- **Logical breakdown**:
  - NOT:
    - `ranSimulation` is true

### Category 2: Ran the simulation with only default values.

- **Feedback**: Hazbot: Looks like you haven’t changed the Setup yet. I can help! [Show me]
- **Expression**: `NOT setAnyZoneVar AND ranSimulation`
- **Logical breakdown**:
  - ALL of:
    - NOT:
      - `setAnyZoneVar` is true
    - `ranSimulation` is true

### Category 3: Ran the simulation with incorrect zone setup values

- **Feedback**: Hazbot: Hmm, it looks like your zone setups do not match the photos. Let's change that so we can compare zones! [Show me]
- **Expression**: `setAnyZoneVar AND ranSimulation WITH NOT CorrectZoneSetup`
- **Logical breakdown**:
  - ALL of:
    - `setAnyZoneVar` is true
    - exists a `ranSimulation` reading where:
      - NOT:
        - sim-prop `CorrectZoneSetup` is true

### Category 4: Ran the simulation with correct zone setups but without one spark in each zone.

- **Feedback**: Hazbot: I don't see a spark in each zone. Let's make sure that both zones have one spark! [Show me]
- **Expression**: `ranSimulation WITH CorrectZoneSetup AND NOT OneSparkPerZone`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - sim-prop `CorrectZoneSetup` is true
      - NOT:
        - sim-prop `OneSparkPerZone` is true

### Category 5: Ran the simulation with correct setups and one spark in each zone

- **Feedback**: Hazbot: Great job! You’re ready to answer the questions below. [Hooray!]
- **Expression**: `ranSimulation WITH CorrectZoneSetup AND OneSparkPerZone`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - sim-prop `CorrectZoneSetup` is true
      - sim-prop `OneSparkPerZone` is true
