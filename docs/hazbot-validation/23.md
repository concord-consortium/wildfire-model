> **AUTO-GENERATED — DO NOT EDIT — re-run `scripts/generate-hazbot-validation-playbook.js`**


# Validation Playbook — Rule Set 23

## Factor Variables

- **ranSimulation** — At least one "SimulationStarted" event was recorded.
- **setTerrainType** — There is at least one "SimulationStarted" event for which the terrain type was set distinct from the default value for any zone.
  - Details: Terrain type is set per zone, in the zones data. Default values (trust but verify) = "Plains" (zone 1), "Plains" (zone 2). <i> means taking the zone index i (the index for the zones data, which is, or must be, an array), 0-based. So i=0 means zone 1 and i=1 means zone 2. If true, then neecessarily ranSimulation=true.
- **setVegetation** — There is at least one "SimulationStarted" event for which the vegetation type was set distinct from the default value for any zone.
  - Details: Vegetation is set per zone, in the zones data. Default values (trust but verify) = "Shrub" (zone 1), "Shrub" (zone 2). If true, then neecessarily ranSimulation=true.
- **setDroughtLevel** — There is at least one "SimulationStarted" event for which the drought level was set distinct from the default value for any zone.
  - Details: Drought level is set per zone, in the zones data. Default values (trust but verify) = "Mild Drought" (zone 1), "Mild Drought" (zone 2). If true, then neecessarily ranSimulation=true.
- **setAnyZoneVar** — setTerrainType OR setVegetation OR setDroughtLevel
- **usedOneSparkPerZone** — There is at least one "SimualtionStarted" event for which two sparks were used with one spark per zone.
  - Details: The sparks array must be of length 2, and one element should have zoneIndex = 0 (zone 1 ) and the other with zoneIdx = 1 (zone 2). <j> means taking the spark index (the index of the sparks data, which is, or must be, an array), 0-based.

## Categories

### Category 1: Did not run the simulation. A.k.a. Click button (before they do anything else)

- **Feedback**: Hazbot: I will analyze your model after you run it! Did you see the instructions at the top of the page? Scroll up! [Okay]
- **Expression**: `NOT ranSimulation`
- **Logical breakdown**:
  - NOT:
    - `ranSimulation` is true

### Category 2: Ran the simulation but did not change any conditions (used default values only)

- **Feedback**: Hazbot: Looks like you haven’t changed the Setup yet. I can help! [Show me]
- **Expression**: `NOT setAnyZoneVar AND ranSimulation`
- **Logical breakdown**:
  - ALL of:
    - NOT:
      - `setAnyZoneVar` is true
    - `ranSimulation` is true

### Category 3: Ran the simulation and changed conditions except the drought level

- **Feedback**: Hazbot: Hmm, it looks like both zones have the same drought level. Let's change that so we can compare zones! [Show me]
- **Expression**: `NOT setDroughtLevel AND setAnyZoneVar`
- **Logical breakdown**:
  - ALL of:
    - NOT:
      - `setDroughtLevel` is true
    - `setAnyZoneVar` is true

### Category 4: Changed the drought level but did not place two sparks (one in each zone)

- **Feedback**: Hazbot: I don't see a spark in each zone. Let's make sure that both zones have one spark! [Show me]
- **Expression**: `setDroughtLevel AND NOT usedOneSparkPerZone`
- **Logical breakdown**:
  - ALL of:
    - `setDroughtLevel` is true
    - NOT:
      - `usedOneSparkPerZone` is true

### Category 5: Ran the simulation, changed drought level, and placed one spark in each zone

- **Feedback**: Hazbot: Great job! You’re ready to answer the questions below. [Hooray!]
- **Expression**: `setDroughtLevel AND usedOneSparkPerZone`
- **Logical breakdown**:
  - ALL of:
    - `setDroughtLevel` is true
    - `usedOneSparkPerZone` is true
