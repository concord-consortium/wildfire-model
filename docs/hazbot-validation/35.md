> **AUTO-GENERATED — DO NOT EDIT — re-run `scripts/generate-hazbot-validation-playbook.js`**


# Validation Playbook — Rule Set 35

## Factor Variables

- **All factor variables and sim props used here already appeared in previvous pages, with the single exception being UniformTerrainTypes.  However, its definition is totally analogous to that of UniformDroughtLevels (see 32).** — (no definition)

## Categories

### Category 1: Did not run the simulation

- **Feedback**: Hazbot: You haven’t run the model yet. Run it before answering the questions.
- **Expression**: `NOT ranSimulation`
- **Logical breakdown**:
  - NOT:
    - `ranSimulation` is true

### Category 2: Ran the simulation but did not change any variables (used default values only)

- **Feedback**: Hazbot: Click “Setup” and change the conditions to compare forest with and without suppression. Then run the model again.
- **Expression**: `ranSimulation AND NOT setAnyVar`
- **Logical breakdown**:
  - ALL of:
    - `ranSimulation` is true
    - NOT:
      - `setAnyVar` is true

### Category 3: Ran the simulation but did not assign forest with and without suppression to each zone

- **Feedback**: Hazbot: This investigation compares forests with and without suppression. Set one zone to forest and the other to forest with suppression.
- **Expression**: `ranSimulation WITH NOT ForestWAWOSuppression`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - NOT:
      - sim-prop `ForestWAWOSuppression` is true

### Category 4: Ran the simulation with 2 vegetations but varied drought between the zones

- **Feedback**: Hazbot: This investigation compares forest types. Make sure the drought is the same for each zone. Then run the model again.
- **Expression**: `ranSimulation WITH ForestWAWOSuppression AND NOT UniformDroughtLevels`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - sim-prop `ForestWAWOSuppression` is true
      - NOT:
        - sim-prop `UniformDroughtLevels` is true

### Category 4: Ran the simulation with 2 vegetations but varied terrain between the zones

- **Feedback**: Hazbot: This investigation compares forest types in the same terrain! Make sure both zones are mountains. Then run the model again.
- **Expression**: `ranSimulation WITH ForestWAWOSuppression AND NOT UniformTerrainTypes`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - sim-prop `ForestWAWOSuppression` is true
      - NOT:
        - sim-prop `UniformTerrainTypes` is true

### Category 5: Ran the simulation with forest with and without suppression, but did not place two sparks (one in each zone)

- **Feedback**: Hazbot: This model has two zones for comparison. Add one spark in each zone so you can compare wildfire spread.
- **Expression**: `ranSimulation WITH ForestWAWOSuppression AND UniformTerrainTypes AND UniformDroughtLevels AND NOT OneSparkPerZone`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - ALL of:
        - ALL of:
          - sim-prop `ForestWAWOSuppression` is true
          - sim-prop `UniformTerrainTypes` is true
        - sim-prop `UniformDroughtLevels` is true
      - NOT:
        - sim-prop `OneSparkPerZone` is true

### Category 6: Ran the simulation, set zones with forest and forest w/ suppression,, and placed one spark in each zone

- **Feedback**: Hazbot: Great job! You set up the model to compare two types of forest. You’re ready to answer the questions below.
- **Expression**: `ranSimulation WITH ForestWAWOSuppression AND UniformTerrainTypes AND UniformDroughtLevels AND OneSparkPerZone`
- **Logical breakdown**:
  - exists a `ranSimulation` reading where:
    - ALL of:
      - ALL of:
        - ALL of:
          - sim-prop `ForestWAWOSuppression` is true
          - sim-prop `UniformTerrainTypes` is true
        - sim-prop `UniformDroughtLevels` is true
      - sim-prop `OneSparkPerZone` is true
