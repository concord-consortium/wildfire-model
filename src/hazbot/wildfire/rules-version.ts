// App-side rules version (per Req 20). Bumped per the policy in
// docs/hazbot-update-workflow.md when the category a given session resolves to changes,
// whether from a sheet edit or from a change in how the expressions are evaluated.
export const APP_RULES_VERSION = 7;
